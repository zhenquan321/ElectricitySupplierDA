var payPage_ctr = function ($scope, $modalInstance, $rootScope, $http, $timeout) {


    $scope.ok = function () {
        $modalInstance.close($scope.selected);
    };
    $scope.cancel = function () {
        $modalInstance.dismiss('cancel');
    };

    //上传头像
  

    //自动调用



}